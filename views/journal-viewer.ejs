<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- Scrapbook Theme CSS -->
    <link href="/css/scrapbook-theme.css" rel="stylesheet">
    
    <style>
        /* Scrapbook Journal Viewer Styles */
        .journal-viewer {
            background: var(--scrapbook-cream);
            min-height: 100vh;
            padding: 2rem 0;
            position: relative;
        }
        
        .journal-header {
            background: var(--scrapbook-aged-paper);
            border-radius: 15px;
            box-shadow: 0 8px 32px var(--scrapbook-shadow-medium);
            padding: 3rem 2rem;
            margin-bottom: 2rem;
            text-align: center;
            position: relative;
            border: 2px solid var(--scrapbook-warm-brown);
        }
        
        .journal-header::before {
            content: '';
            position: absolute;
            top: -4px;
            left: -4px;
            right: -4px;
            bottom: -4px;
            background: linear-gradient(45deg, var(--scrapbook-gold), var(--scrapbook-amber), var(--scrapbook-gold));
            border-radius: 17px;
            z-index: -1;
            opacity: 0.3;
        }
        
        .journal-nav {
            background: var(--scrapbook-parchment);
            border-radius: 15px;
            box-shadow: 0 4px 20px var(--scrapbook-shadow-light);
            padding: 2rem;
            margin-bottom: 2rem;
            border: 1px solid var(--scrapbook-warm-brown);
        }
        
        .journal-page {
            background: var(--scrapbook-aged-paper);
            border-radius: 15px;
            box-shadow: 0 8px 32px var(--scrapbook-shadow-medium);
            padding: 3rem;
            margin-bottom: 2rem;
            min-height: 600px;
            position: relative;
            border: 2px solid var(--scrapbook-warm-brown);
        }
        
        .journal-page::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(45deg, 
                var(--scrapbook-warm-brown) 0%, 
                var(--scrapbook-rust) 25%, 
                var(--scrapbook-burgundy) 50%, 
                var(--scrapbook-warm-brown) 75%, 
                var(--scrapbook-deep-brown) 100%);
            border-radius: 17px;
            z-index: -1;
            opacity: 0.2;
        }
        
        .journal-content {
            margin-top: 1rem;
        }
        
        .content-item {
            margin-bottom: 2.5rem;
            padding: 1.5rem;
            border-radius: 12px;
            background: var(--scrapbook-cream);
            border: 1px solid var(--scrapbook-warm-brown);
            position: relative;
            box-shadow: 0 2px 10px var(--scrapbook-shadow-light);
        }
        
        .content-item.heading {
            background: linear-gradient(135deg, var(--scrapbook-burgundy), var(--scrapbook-rust));
            color: var(--scrapbook-cream);
            border: 2px solid var(--scrapbook-deep-brown);
            text-align: center;
        }
        
        .content-item.text {
            background: var(--scrapbook-parchment);
            border-left: 4px solid var(--scrapbook-forest-green);
        }
        
        .content-item.menu {
            background: var(--scrapbook-cream);
            border-left: 4px solid var(--scrapbook-burnt-orange);
        }
        
        .content-item.photo {
            background: var(--scrapbook-parchment);
            border-left: 4px solid var(--scrapbook-gold);
        }
        
        .content-item.page_photo {
            background: var(--scrapbook-cream);
            border-left: 4px solid var(--scrapbook-amber);
        }
        
        .content-item.blog {
            background: var(--scrapbook-parchment);
            border-left: 4px solid var(--scrapbook-maroon);
        }
        
        .year-selector {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            justify-content: center;
        }
        
        .year-btn {
            padding: 0.75rem 1.5rem;
            border: 2px solid var(--scrapbook-warm-brown);
            background: var(--scrapbook-cream);
            color: var(--scrapbook-warm-brown);
            border-radius: 25px;
            text-decoration: none;
            transition: all 0.3s ease;
            font-family: var(--font-casual);
            font-weight: 600;
            box-shadow: 0 2px 8px var(--scrapbook-shadow-light);
        }
        
        .year-btn:hover, .year-btn.active {
            background: var(--scrapbook-warm-brown);
            color: var(--scrapbook-cream);
            transform: translateY(-2px);
            box-shadow: 0 4px 15px var(--scrapbook-shadow-medium);
        }
        
        .page-nav {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin-top: 2rem;
        }
        
        .page-btn {
            padding: 0.75rem 1.5rem;
            border: 2px solid var(--scrapbook-deep-brown);
            background: var(--scrapbook-cream);
            color: var(--scrapbook-deep-brown);
            border-radius: 25px;
            text-decoration: none;
            transition: all 0.3s ease;
            font-family: var(--font-casual);
            font-weight: 600;
            box-shadow: 0 2px 8px var(--scrapbook-shadow-light);
        }
        
        .page-btn:hover, .page-btn.active {
            background: var(--scrapbook-deep-brown);
            color: var(--scrapbook-cream);
            transform: translateY(-2px);
            box-shadow: 0 4px 15px var(--scrapbook-shadow-medium);
        }
        
        .loading {
            text-align: center;
            padding: 3rem;
            color: var(--scrapbook-text-medium);
            font-family: var(--font-casual);
            font-size: 1.2rem;
        }
        
        .no-content {
            text-align: center;
            padding: 3rem;
            color: var(--scrapbook-text-medium);
            font-family: var(--font-casual);
        }
        
        .journal-image {
            max-width: 100%;
            height: auto;
            border-radius: 12px;
            box-shadow: 
                0 4px 15px var(--scrapbook-shadow-medium),
                0 0 0 8px var(--scrapbook-cream),
                0 0 0 10px var(--scrapbook-warm-brown);
            display: block;
            margin: 1rem auto;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .journal-image:hover {
            transform: scale(1.02);
            box-shadow: 
                0 6px 20px var(--scrapbook-shadow-medium),
                0 0 0 8px var(--scrapbook-cream),
                0 0 0 10px var(--scrapbook-burgundy);
        }
        
        .menu-image {
            width: 100%;
            height: auto;
            object-fit: contain;
        }
        
        .photo-image {
            width: 33.33%;
            height: auto;
            object-fit: contain;
        }
        
        .page-photo-image {
            width: 100%;
            height: auto;
            object-fit: contain;
        }
        
        .blog-image {
            width: 100%;
            height: auto;
            object-fit: contain;
        }
        
        /* Scrapbook decorative elements */
        .content-item::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            width: 15px;
            height: 15px;
            background: var(--scrapbook-gold);
            opacity: 0.6;
            clip-path: polygon(0 0, 100% 0, 0 100%);
        }
        
        .content-item::after {
            content: '';
            position: absolute;
            bottom: -2px;
            right: -2px;
            width: 15px;
            height: 15px;
            background: var(--scrapbook-gold);
            opacity: 0.6;
            clip-path: polygon(100% 0, 100% 100%, 0 100%);
        }
        
        @media (max-width: 768px) {
            .journal-viewer {
                padding: 1rem 0;
            }
            
            .journal-header, .journal-nav, .journal-page {
                margin-bottom: 1rem;
                padding: 1.5rem;
            }
            
            .year-selector {
                flex-direction: column;
                align-items: center;
            }
            
            .photo-image {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="journal-viewer">
        <div class="container">
            <!-- Header -->
            <div class="journal-header">
                <h1 class="scrapbook-title">
                    <i class="fas fa-book-open me-3"></i>
                    Thanksgiving Journal
                </h1>
                <p class="scrapbook-subtitle">Browse through our Thanksgiving memories year by year</p>
            </div>
            
            <!-- Year Navigation -->
            <div class="journal-nav">
                <h5 class="scrapbook-subtitle text-center mb-3">
                    <i class="fas fa-calendar-alt me-2"></i>
                    Select a Year
                </h5>
                <div class="year-selector" id="yearSelector">
                    <div class="scrapbook-loading">
                        <i class="fas fa-spinner fa-spin me-2"></i>
                        Loading years...
                    </div>
                </div>
            </div>
            
            <!-- Journal Content -->
            <div id="journalContent">
                <div class="no-content">
                    <i class="fas fa-book-open fa-3x mb-3" style="color: var(--scrapbook-text-light);"></i>
                    <h4 class="scrapbook-subtitle">Select a year to view journal pages</h4>
                    <p class="scrapbook-text">Choose a year from the navigation above to see our Thanksgiving memories</p>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Journal Viewer JavaScript -->
    <script src="/js/components/journalViewer.js"></script>
</body>
</html>
